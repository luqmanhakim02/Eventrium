import React from "react";
import ReactDOM from "react-dom";
import JoinEvent from "./join-event";

ReactDOM.render(
  <React.StrictMode>
    <JoinEvent />
  </React.StrictMode>,
  document.getElementById("join-event")
);
